# Log

> Append-only record of operations on the wiki. Each entry is prefixed
> `## [YYYY-MM-DD] <op> | <title>` where `<op>` is `ingest`, `query`, `lint`,
> `build`, etc., so entries stay greppable. Newest entries go at the bottom.
> Never rewrite history here.

<!-- First entry goes below once you ingest your first source, e.g.

## [YYYY-MM-DD] ingest | <source title>

Created: sources/<slug>.md, concepts/<x>.md, decisions/0001-<slug>.md
Updated: architecture.md, index.md
-->
